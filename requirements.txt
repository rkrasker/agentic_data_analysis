# Core dependencies
pyyaml>=6.0
pytest>=7.4.0
pytest-cov>=4.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# LangChain core (wide version range for 0.2.x+ compatibility)
langchain-core>=0.2.0,<2.0.0

# LLM Providers - install what you need
# Gemini (Google AI)
langchain-google-genai>=1.0.0

# Claude (Anthropic) - uncomment when needed
# langchain-anthropic>=0.1.0

# OpenAI - uncomment when needed
# langchain-openai>=0.1.0
